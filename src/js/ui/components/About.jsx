'use strict';

const React = require('react');

const About = (props) => {
    return (
        <div className="message">
            AstroFox version 1.0
        </div>
    );
};

module.exports = About;