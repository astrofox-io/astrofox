'use strict';

var THREE = require('three');
var fs = global.require('fs');

var SHADER_PATH = '';

var ShaderLoader = {
    load: function(name) {
        // TODO
    }
};

module.exports = ShaderLoader;